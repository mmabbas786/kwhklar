{
  "name": "kwhklar",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test": "vitest run",
    "audit:seo": "tsx scripts/audit-seo.ts && tsx scripts/reconcile-sitemap.ts",
    "validate:parity": "tsx scripts/validate-locale-parity.ts && tsx scripts/validate-navigation-parity.ts",
    "validate:all": "npm run check && npm run test && npm run validate:parity && npm run audit:seo"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.4.1",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^26.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
