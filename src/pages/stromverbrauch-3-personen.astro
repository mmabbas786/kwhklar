---
import BaseLayout from '../layouts/BaseLayout.astro';
import UniversalCalculator from '../components/calculator/UniversalCalculator.astro';
import Breadcrumbs from '../components/seo/Breadcrumbs.astro';
import RelatedLinks from '../components/seo/RelatedLinks.astro';
import SourceBox from '../components/trust/SourceBox.astro';
import FaqSection from '../components/content/FaqSection.astro';
import { householdBenchmarks } from '../data/household-benchmarks';
import { formatKwh } from '../lib/formatters';

const data = householdBenchmarks[3];

const faqs = [
  {
    question: 'Was kostet der Strom für 3 Personen im Monat?',
    answer: 'Ein 3-Personen-Haushalt mit einem Verbrauch von 2.600 bis 3.500 kWh zahlt bei 35 ct/kWh ca. 90 bis 115 Euro monatlich (ca. 1.050 bis 1.350 € im Jahr).',
  },
];

const related = [
  { title: '2-Personen-Haushalt', href: '/stromverbrauch-2-personen', description: 'Richtwerte für 2 Personen.' },
  { title: '4-Personen-Haushalt', href: '/stromverbrauch-4-personen', description: 'Richtwerte für 4 Personen.' },
  { title: 'Stromrechnung prüfen', href: '/stromrechnung-pruefen', description: 'Abrechnung kontrollieren.' },
];
---

<BaseLayout
  title="Stromverbrauch 3 Personen: Durchschnitt & Kosten | kWhKlar"
  description="Wie viel Strom verbraucht ein 3-Personen-Haushalt? Offizielle Richtwerte (2.600–3.700 kWh), Kosten und Einsparpotenziale für kleine Familien & WGs."
  canonical="https://kwhklar.de/stromverbrauch-3-personen"
  breadcrumbs={[
    { name: 'Haushalt', path: '/stromverbrauch-haushalt' },
    { name: '3 Personen', path: '/stromverbrauch-3-personen' },
  ]}
>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
    <Breadcrumbs
      items={[
        { name: 'Haushalt', path: '/stromverbrauch-haushalt' },
        { name: '3 Personen', path: '/stromverbrauch-3-personen' },
      ]}
    />

    <div class="max-w-3xl mb-8">
      <span class="badge-emerald text-xs mb-2">3-Personen Benchmark</span>
      <h1 class="text-3xl sm:text-4xl font-extrabold text-slate-900 tracking-tight">
        Stromverbrauch für 3 Personen (Kleine Familie / WG)
      </h1>
      <p class="mt-3 text-base text-slate-600 leading-relaxed">
        {data.description}
      </p>
    </div>

    <!-- Benchmark cards -->
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-5 mb-10">
      <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-xs">
        <span class="text-xs text-slate-500 font-medium">Wohnung (ohne Warmwasser)</span>
        <div class="mt-2 text-2xl font-bold text-slate-900">{formatKwh(data.apartmentWithoutWarmWaterKwh, 0)}</div>
        <span class="text-xs text-slate-400 mt-1 block">ca. 85–95 € / Monat</span>
      </div>

      <div class="rounded-2xl border border-amber-200 bg-amber-50/50 p-5 shadow-xs">
        <span class="text-xs text-amber-800 font-medium">Wohnung (mit Warmwasser elektrisch)</span>
        <div class="mt-2 text-2xl font-bold text-amber-950">{formatKwh(data.apartmentWithWarmWaterKwh, 0)}</div>
        <span class="text-xs text-amber-700 mt-1 block">ca. 120–135 € / Monat</span>
      </div>

      <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-xs">
        <span class="text-xs text-slate-500 font-medium">Einfamilienhaus</span>
        <div class="mt-2 text-2xl font-bold text-slate-900">{formatKwh(data.houseWithoutWarmWaterKwh, 0)}</div>
        <span class="text-xs text-slate-400 mt-1 block">ca. 115–130 € / Monat</span>
      </div>
    </div>

    <UniversalCalculator
      defaultWatt={350}
      defaultHours={24}
      defaultPriceCt={35.0}
      title="3-Personen Verbrauchsrechner"
      subtitle="Ermittle deine Stromkosten bei individuellem Verbrauch:"
    />

    <RelatedLinks links={related} />
    <SourceBox sourceIds={['stromspiegel', 'bdew']} />
    <FaqSection items={faqs} />
  </div>
</BaseLayout>
